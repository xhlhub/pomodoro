# Ensure proper native module rebuilding
runtime=electron
target=28.0.0
disturl=https://electronjs.org/headers

# Use official npm registry to avoid mirror issues
registry=https://registry.npmjs.org/ 